[package]
name = "entity"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "entity"
path = "src/lib.rs"

[dependencies]
sea-orm = { version = "0.7", default-features = false, features = ["sqlx-mysql", "runtime-tokio-rustls", "macros", "debug-print"] }
